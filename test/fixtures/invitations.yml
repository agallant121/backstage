pending:
  group: pending_group
  inviter: one
  email: one@example.com
  token: pending-token
  expires_at: <%= 14.days.from_now %>

used:
  group: pending_group
  inviter: one
  invited_user: one
  email: one@example.com
  token: used-token
  accepted_at: <%= 1.day.ago %>
  expires_at: <%= 13.days.from_now %>
