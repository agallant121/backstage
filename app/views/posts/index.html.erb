<div class="d-flex justify-content-between align-items-center mb-3 flex-wrap gap-2">
  <div>
    <p class="text-uppercase text-muted small mb-1"><%= "Updates" %></p>
    <h1 class="h4 mb-0"><%= "All posts from your groups" %></h1>
  </div>
  <div class="d-flex flex-wrap gap-2">
    <%= link_to "New Post", new_post_path, class: "btn btn-primary" %>
  </div>
</div>

<%= turbo_frame_tag "posts_list" do %>
  <% toggle_path = @view_mode == :full ? posts_path : posts_path(view: "full") %>
  <%= render "post_list",
             posts: @posts,
             view_mode: @view_mode,
             toggle_path: toggle_path,
             show_group_badges: true,
             show_actions: true,
             empty_state_message: "No posts yet. Share something to kick things off!" %>
<% end %>
