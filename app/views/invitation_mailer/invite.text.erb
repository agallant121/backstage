<%= "Hello," %>

<%= "#{@inviter.display_name} invited you to join the #{@group.name} group." %>

<%= "Accept the invite and finish signing up here: #{@accept_url}" %>
<%= "This invite link expires on #{I18n.l(@expires_at.to_date, format: :long)}." %>

<%= "If you didn't expect this email, you can ignore it." %>
